interface MetaDoc {
    name: string;
    order: number;
}
interface MetaGroup {
    name: string;
    docs: MetaDoc[];
}
export interface Meta {
    groups: MetaGroup[];
}
export declare const DEFAULT_GROUP_NAME = "_DEFAULT_GROUP_";
export declare function getGroup(text: string): string | null;
export declare function parse(content: string | null): Meta;
export {};
