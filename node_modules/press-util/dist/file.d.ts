/// <reference types="node" />
import { Dirent } from 'fs';
/**
 * 判断文件是否为文档
 * @param dirent
 * @returns
 */
export declare function isDocFile(dirent: Dirent): boolean;
/**
 * 判断是否为有效目录
 * @param dirent
 * @returns
 */
export declare function isValidDir(dirent: Dirent): boolean;
interface Doc {
    name: string;
    file: Dirent;
}
interface DocGroup {
    name: string;
    docs: Doc[];
    dirs: Dirent[];
}
export declare function readDocGroups(path: string): DocGroup[];
export {};
