interface BaseOptions {
    token: string;
    pid: string;
}
interface DownloadOptions extends BaseOptions {
    destDir: string;
}
interface DownloadSvgsOptions extends BaseOptions {
    filename: (iconName: string) => string;
    destDir: string;
}
export declare function download(options: DownloadOptions): Promise<void>;
export declare function downloadSvgs(options: DownloadSvgsOptions): Promise<void>;
export {};
